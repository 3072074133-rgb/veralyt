# 贡献指南

感谢你参与 Veralyt。建议先搜索现有 Issue；较大的功能变更请先创建 Issue 说明问题、目标和设计边界。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Python 3.13、uv、Node.js 20+ 和 npm 安装依赖。
3. 修改后运行后端测试、Ruff、前端单元测试和类型检查。
4. 提交 Pull Request，说明行为变化、验证方式和已知限制。
5. 涉及提示词时，同时补充一个固定输入测试，并说明模型调用是否改变。
6. 不要提交本地数据库、上传文件、模型缓存或构建产物。

提交信息请使用简洁的祈使句，Pull Request 应聚焦一个问题。界面改动请附截图，协议或 API 改动请同步更新类型和文档。

## 设计边界

- 普通问题直接回答，不要加入报表查询流程。
- 报表中的数字必须来自工具结果或可复算的计算证据。
- 查询必须是只读操作，并经过后端字段和行数校验。
- 不保存节点输入、输出、提示词和模型思考过程。
- 新增流程前，优先确认是否可以放入现有节点，而不是增加新的工作流分支。

提交即表示你同意按项目的 MIT License 授权贡献内容，并遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。
