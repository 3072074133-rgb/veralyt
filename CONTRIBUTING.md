# 贡献指南

## 开发流程

1. 使用 Python 3.13 和 Node.js 安装依赖。
2. 修改后运行后端测试、Ruff、前端单元测试和类型检查。
3. 涉及提示词时，同时补充一个固定输入测试，并说明模型调用是否改变。
4. 不要提交本地数据库、上传文件、模型缓存或构建产物。

## 设计边界

- 普通问题直接回答，不要加入报表查询流程。
- 报表中的数字必须来自工具结果或可复算的计算证据。
- 查询必须是只读操作，并经过后端字段和行数校验。
- 不保存节点输入、输出、提示词和模型思考过程。
- 新增流程前，优先确认是否可以放入现有节点，而不是增加新的工作流分支。
