# 架构说明

## 两条执行路径

系统先执行一次意图识别：

```text
普通问题 ──> respond_node ──> END
报表分析 ──> prepare_context ──> plan ──> execute
                              ──> draft ──> validate ──> finish
```

普通问题不读取数据集、不调用查询工具，也不执行证据复核。分类模型必须在意图结果中直接提供完整回答，后端不会补写或替换。

报表路径中的工具、数据表、查询规格、结论和图表均由模型选择。后端绑定可信数据集，校验字段、证据坐标和只读查询，并执行模型明确指定的计算；后端不生成、删除或改写分析结论。

## 执行边界

后端不按关键词、固定指标、相似度、阈值或预设优先级决定意图、数据表、工具、查询、结论、图表和知识内容，也不在模型输出后补齐、缩减或替换这些决定。模型输出缺字段、引用不存在的数据、生成不安全查询或超过资源上限时，本轮明确失败；程序不会改成另一种策略继续执行。

仍保留的确定性代码只负责文件结构解析、用户显式数据修改、只读 SQL 安全、字段与证据坐标真实性、任务状态、并发、超时、行数和调用次数限制，以及模型明确选择的工具计算。它们不读取问题或结论文字来判断业务含义，也不选择分析策略。

## 数据边界

- `tool_results` 保存本轮执行状态，供工作流继续运行。
- `evidence` 保存可追溯的原始行和计算输入。
- `run_artifacts` 保存摘要、查询参数、行数、证据 ID 和图表定义，不重复保存查询原始行。
- 节点输入、输出、提示词和模型思考过程不持久化。

## 扩展点

长期记忆、报告发布和导出能力通过现有服务层提供，核心路径不依赖这些扩展。意图识别始终由模型完成，不再提供关键词规则兼容模式。模型输出无效或服务不可用时，运行明确失败，可重试。泛化请求和重试请求均交给模型结合会话上下文规划。
