# 架构说明

## 两条执行路径

系统先执行一次意图识别：

```text
普通问题 ──> respond_node ──> END
报表分析 ──> prepare_context ──> plan ──> execute ──> assess_results
                                    ↑             │
                                    └── replan ───┤
                                                  └──> draft ──> final_review ──> finish
```

普通问题不读取数据集、不调用查询工具，也不执行证据复核。分类模型必须在意图结果中直接提供完整回答，后端不会补写或替换。

报表路径中，规划模型读取全部数据表的物理表名、完整 Schema、来源区域和有界样例行，直接生成 DuckDB SQL。单表查询和多表 JOIN、UNION、CTE 走同一个 `execute_sql` 执行器；系统没有按报表名或固定字段名绑定的专项分析工具。查询结果充分性、结论、图表和最终交付决定均由模型选择。后端校验只读 SQL 与任务表作用域、执行计算并解析可用引用，不判断或否决模型认可的分析结论。

## 执行边界

后端不按关键词、固定指标、相似度、阈值或预设优先级决定意图、数据表、工具、查询、结论、图表和知识内容，也不在模型输出后补齐、缩减或替换这些决定。模型认可报告后直接交付；无法解析的引用保留为展示缺口，不构成交付否决。结构化模型输出无效、生成不安全查询或超过技术资源上限时，本轮明确失败。

仍保留的确定性代码只负责文件结构解析、用户显式数据修改、只读 SQL 安全、任务数据表作用域、引用解析、任务状态、并发、超时、行数和调用次数限制，以及原样执行模型生成的 SQL。它们不读取问题或结论文字来判断业务含义，也不选择分析策略或报告能否交付。

## 数据边界

- `tool_results` 保存本轮执行状态，供工作流继续运行。
- `evidence` 保存可追溯的原始行和计算输入。
- `run_artifacts` 保存摘要、查询参数、行数、证据 ID 和图表定义，不重复保存查询原始行。
- 节点输入、输出、提示词和模型思考过程不持久化。

## 扩展点

长期记忆、报告发布和导出能力通过现有服务层提供，核心路径不依赖这些扩展。意图识别始终由模型完成，不再提供关键词规则兼容模式。模型输出无效或服务不可用时，运行明确失败，可重试。泛化请求和重试请求均交给模型结合会话上下文规划。

运行时模型配置由 `model_settings` 服务统一提供。未保存配置时读取环境中的 Ollama 默认值；保存后从 `data/model-settings.json` 热加载。模型网关按每次调用时的有效配置选择 Ollama 原生接口或 OpenAI 兼容 `/chat/completions` 接口，因此切换供应商无需重启。设置 API 只暴露掩码密钥，连接测试使用候选配置但不写入活动配置。
